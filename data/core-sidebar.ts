import { RouteItem } from '../data/get-route-context'

const sidebar = {
  routes: [
    {
      title: 'Get Pro Account',
      path: '/dashboard',
    }

  ] as RouteItem[],
}

export default sidebar
